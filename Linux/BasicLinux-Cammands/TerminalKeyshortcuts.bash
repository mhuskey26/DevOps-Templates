# TAB = Use the TAB key for auto fill a cmd, if you get beep that means its not able to locat the cmd or dosn't have enough info. You can also use TAB to complete a directory search as well
Examples: cat /e then hit TAB you will see it auto fill to cat /etc/ since there is only one directory starting with /e
# But what if there where two like /etc and a /efs for exzample it will beep, you will need to give it a little more to be able to autofill to fined all the simlare ones you can hit TAB twice
# Ctl+E = Moves the cursur to the end of the line for the cmd
# Ctl+A = Move the cursur to the beging of the cmd
# Ctl+U = Erases the full cmd you entered, helpful for if you misspelled a password
# Clt+C = Kills a running cmd like endless ping or a process like a larg download
# Alt+n = adds a : to the cmd
# Ctl+v = past a cmd
# Up arrow key = will input past cmds
# cmd+TABx2 = Will show all the cmds for that command
# Ctl+l = puts current cmd line to the top
# Ctl+D = Exit the terminal
# 

